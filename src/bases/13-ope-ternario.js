// Operador ternario in JS

const condition = true;

// const message = ( condition ) ? 'Activo' : 'Inactivo';
// const message = ( condition ) ? 'Activo' : null;
const message = ( condition ) && 'Activo';

console.log(message);
